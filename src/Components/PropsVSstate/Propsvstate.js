import React from 'react';

const Propsvstate = () => {
    return (
        <div>
            <h2 style={{ textAlign: 'center' }}>What is the difference between props vs state in React?</h2>
        </div>
    );
};

export default Propsvstate;